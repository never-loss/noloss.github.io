{
  "name": "never-loss",
  "version": "0.1.0",
  "private": true,
  "engines": { "node": ">=22.18.0" },
  "scripts": {
    "test": "node --test \"tests/**/*.test.ts\"",
    "listen": "node src/cli/listen.ts"
  }
}
